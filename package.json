{
  "name": "collect-emails-to-document",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot index.ts",
    "build": "bun build ./index.ts --compile --outfile server",
    "start": "bun run ./index.ts",
    "lint": "biome lint .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.7.4",
    "hono-pino": "^0.8.0",
    "lefthook": "^1.11.3",
    "mongodb": "^6.14.2",
    "mongoose": "^8.12.1",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "zod": "^3.24.2"
  }
}
